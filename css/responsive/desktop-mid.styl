// ******************************************************

// GRID

$col = 70px
$gutter = 14px

.container
  margin: 0 auto
  width: ($col * $cols) + ($gutter * $cols)

.col
  margin-left: ($gutter/2)
  margin-right: ($gutter/2)

for col in 1..$cols
  .col-{col}
    width: ( col * $col ) + ( ( col - 1 ) * $gutter )